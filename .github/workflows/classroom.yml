name: Autograding Tests
'on':
  - push
  - repository_dispatch
permissions:
  checks: write
  actions: read
  contents: read

jobs:
  # JOB: ОЦЕНКА ЗАДАНИЙ (3 параллельных проверки)
  run-autograding-tests:
    name: Оценивание всех задач
    runs-on: ubuntu-latest
    if: github.actor != 'github-classroom[bot]'
    
    strategy:
      matrix:
        task: [1, 2, 3]
      max-parallel: 3
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'
    
    # ============ ЗАДАЧА 1: КАЛЬКУЛЯТОР ============
    - name: "[TASK 1] Сложение"
      if: matrix.task == 1
      id: task1_add
      uses: classroom-resources/autograding-io-grader@v1
      with:
        test-name: "Задача 1: Сложение"
        command: python task_01.py
        input: |
          5
          +
          3
        expected-output: '8'
        comparison-method: exact
        timeout: 5
        max-score: 10
    
    - name: "[TASK 1] Вычитание"
      if: matrix.task == 1
      id: task1_sub
      uses: classroom-resources/autograding-io-grader@v1
      with:
        test-name: "Задача 1: Вычитание"
        command: python task_01.py
        input: |
          10
          -
          4
        expected-output: '6'
        comparison-method: exact
        timeout: 5
        max-score: 10
    
    - name: "[TASK 1] Деление на ноль"
      if: matrix.task == 1
      id: task1_div_zero
      uses: classroom-resources/autograding-io-grader@v1
      with:
        test-name: "Задача 1: Деление на ноль"
        command: python task_01.py
        input: |
          10
          /
          0
        expected-output: 'error'
        comparison-method: contains
        timeout: 5
        max-score: 10
    
    # ============ ЗАДАЧА 2: ПАЛИНДРОМ ============
    - name: "[TASK 2] Простой палиндром"
      if: matrix.task == 2
      id: task2_simple
      uses: classroom-resources/autograding-io-grader@v1
      with:
        test-name: "Задача 2: Простой палиндром"
        command: python task_02.py
        input: "racecar"
        expected-output: 'True'
        comparison-method: exact
        timeout: 5
        max-score: 15
    
    - name: "[TASK 2] Не палиндром"
      if: matrix.task == 2
      id: task2_not_palindrome
      uses: classroom-resources/autograding-io-grader@v1
      with:
        test-name: "Задача 2: Не палиндром"
        command: python task_02.py
        input: "hello"
        expected-output: 'False'
        comparison-method: exact
        timeout: 5
        max-score: 15
    
    - name: "[TASK 2] Пустая строка"
      if: matrix.task == 2
      id: task2_empty
      uses: classroom-resources/autograding-io-grader@v1
      with:
        test-name: "Задача 2: Пустая строка"
        command: python task_02.py
        input: ""
        expected-output: 'True'
        comparison-method: exact
        timeout: 5
        max-score: 10
    
    # ============ ЗАДАЧА 3: ФИБОНАЧЧИ ============
    - name: "[TASK 3] Fibonacci(0)"
      if: matrix.task == 3
      id: task3_fib0
      uses: classroom-resources/autograding-io-grader@v1
      with:
        test-name: "Задача 3: Fibonacci(0)"
        command: python task_03.py
        input: "0"
        expected-output: '0'
        comparison-method: exact
        timeout: 5
        max-score: 10
    
    - name: "[TASK 3] Fibonacci(1)"
      if: matrix.task == 3
      id: task3_fib1
      uses: classroom-resources/autograding-io-grader@v1
      with:
        test-name: "Задача 3: Fibonacci(1)"
        command: python task_03.py
        input: "1"
        expected-output: '1'
        comparison-method: exact
        timeout: 5
        max-score: 10
    
    - name: "[TASK 3] Fibonacci(10)"
      if: matrix.task == 3
      id: task3_fib10
      uses: classroom-resources/autograding-io-grader@v1
      with:
        test-name: "Задача 3: Fibonacci(10)"
        command: python task_03.py
        input: "10"
        expected-output: '55'
        comparison-method: exact
        timeout: 5
        max-score: 20
    
    # ============ ПАРСИМ РЕАЛЬНЫЕ БАЛЛЫ ============
    - name: Parse Real Scores
      if: always()
      run: |
        if [ "${{ matrix.task }}" = "1" ]; then
          # Парсим баллы из autograding результатов для задачи 1
          python3 tools/parse_scores.py 1 \
            "${{ steps.task1_add.outputs.result }}" \
            "${{ steps.task1_sub.outputs.result }}" \
            "${{ steps.task1_div_zero.outputs.result }}" > task_score.txt
          
        elif [ "${{ matrix.task }}" = "2" ]; then
          # Для задачи 2
          python3 tools/parse_scores.py 2 > task_score.txt
          
        elif [ "${{ matrix.task }}" = "3" ]; then
          # Для задачи 3
          python3 tools/parse_scores.py 3 > task_score.txt
        fi
        
        # echo "Task ${{ matrix.task }} score: $SCORE"
        echo "Task ${{ matrix.task }} score:"
        cat task_score.txt
    
    - name: Upload Score Artifact
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: score-task-${{ matrix.task }}
        path: task_score.txt

  # JOB: СБОРКА ИТОГОВОЙ СВОДКИ
  generate-final-report:
    name: Итоговая сводка
    runs-on: ubuntu-latest
    needs: grade-tasks
    if: always()
    
    steps:
    - name: Checkout code again
      uses: actions/checkout@v4
    
    - name: Download all score artifacts
      uses: actions/download-artifact@v4
      with:
        path: ./artifacts
        pattern: score-task-*
        merge-multiple: true

    - name: Calculate Total Scores
      id: calculate
      run: |
        # Инициализируем
        TASK1_SCORE=0
        TASK2_SCORE=0
        TASK3_SCORE=0
        
        # Читаем баллы из файлов
        for file in $(find ./artifacts -name "task_score.txt"); do
          score=$(cat "$file" | grep "TASK_SCORE=" | cut -d'=' -f2)
          score=${score:-0}
          
          if [[ "$file" == *"task-1"* ]]; then
            TASK1_SCORE=$score
          elif [[ "$file" == *"task-2"* ]]; then
            TASK2_SCORE=$score
          elif [[ "$file" == *"task-3"* ]]; then
            TASK3_SCORE=$score
          fi
        done
        
        # Если Task 1 прошел все 3 теста по 10 баллов каждый, должно быть 30
        if [ "$TASK1_SCORE" -lt 30 ] && [ -f "task_01.py" ]; then
          # Проверяем, может быть студент сдал не все тесты
          # Но если файл есть, даем минимум 10
          TASK1_SCORE=$((TASK1_SCORE > 0 ? TASK1_SCORE : 10))
        fi
        
        echo "Final scores: Task1=$TASK1_SCORE, Task2=$TASK2_SCORE, Task3=$TASK3_SCORE"
        
        # Сохраняем в outputs
        echo "task1_score=$TASK1_SCORE" >> $GITHUB_OUTPUT
        echo "task2_score=$TASK2_SCORE" >> $GITHUB_OUTPUT
        echo "task3_score=$TASK3_SCORE" >> $GITHUB_OUTPUT
        echo "total_score=$((TASK1_SCORE + TASK2_SCORE + TASK3_SCORE))" >> $GITHUB_OUTPUT
        echo "max_score=110" >> $GITHUB_OUTPUT

    - name: Generate Final Summary
      run: |
        TASK1_SCORE=${{ steps.calculate.outputs.task1_score || 0 }}
        TASK2_SCORE=${{ steps.calculate.outputs.task2_score || 0 }}
        TASK3_SCORE=${{ steps.calculate.outputs.task3_score || 0 }}
        TOTAL_SCORE=${{ steps.calculate.outputs.total_score || 0 }}
        MAX_SCORE=${{ steps.calculate.outputs.max_score || 110 }}
        
        # Расчет процента
        if [ "$MAX_SCORE" -gt 0 ]; then
          PERCENTAGE=$((TOTAL_SCORE * 100 / MAX_SCORE))
        else
          PERCENTAGE=0
        fi
        
        # Генерация отчета
        echo "## 📊 ИТОГОВЫЙ ОТЧЕТ ПО ВСЕМ ЗАДАНИЯМ" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        echo "### 📈 Сводная таблица" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "| Задание | Баллы | Максимум | Статус |" >> $GITHUB_STEP_SUMMARY
        echo "|---------|-------|----------|--------|" >> $GITHUB_STEP_SUMMARY
        echo "| **Задача 1: Калькулятор** | $TASK1_SCORE | 30 | $(if [ $TASK1_SCORE -ge 24 ]; then echo "✅"; elif [ $TASK1_SCORE -ge 18 ]; then echo "⚠️"; else echo "❌"; fi) |" >> $GITHUB_STEP_SUMMARY
        echo "| **Задача 2: Палиндром** | $TASK2_SCORE | 40 | $(if [ $TASK2_SCORE -ge 32 ]; then echo "✅"; elif [ $TASK2_SCORE -ge 24 ]; then echo "⚠️"; else echo "❌"; fi) |" >> $GITHUB_STEP_SUMMARY
        echo "| **Задача 3: Числа Фибоначчи** | $TASK3_SCORE | 40 | $(if [ $TASK3_SCORE -ge 32 ]; then echo "✅"; elif [ $TASK3_SCORE -ge 24 ]; then echo "⚠️"; else echo "❌"; fi) |" >> $GITHUB_STEP_SUMMARY
        echo "| **ВСЕГО** | **$TOTAL_SCORE** | **$MAX_SCORE** | **$PERCENTAGE%** |" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        echo "### 📁 Найденные файлы:" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [ -f "task_01.py" ]; then
          echo "✅ **task_01.py** - найден" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ **task_01.py** - не найден" >> $GITHUB_STEP_SUMMARY
        fi
        
        if [ -f "task_02.py" ]; then
          echo "✅ **task_02.py** - найден" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ **task_02.py** - не найден" >> $GITHUB_STEP_SUMMARY
        fi
        
        if [ -f "task_03.py" ]; then
          echo "✅ **task_03.py** - найден" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ **task_03.py** - не найден" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        
        echo "### 🏆 Итоговая оценка: **$TOTAL_SCORE / $MAX_SCORE**" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [ "$TOTAL_SCORE" -eq "$MAX_SCORE" ]; then
          echo "🎉 **ПОЗДРАВЛЯЕМ! Все задачи выполнены на 100%!**" >> $GITHUB_STEP_SUMMARY
        elif [ "$PERCENTAGE" -ge 80 ]; then
          echo "👍 **Отличный результат! Почти все задачи выполнены.**" >> $GITHUB_STEP_SUMMARY
        elif [ "$PERCENTAGE" -ge 60 ]; then
          echo "⚠️ **Хорошая работа, но есть куда расти.**" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ **Нужно серьезно поработать над заданиями.**" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "*Автоматическая проверка завершена* • $(date)" >> $GITHUB_STEP_SUMMARY
    
    - name: Report to GitHub Classroom
      uses: classroom-resources/autograding-grading-reporter@v1
      env:
        TASK_01_RESULTS: '{"score": ${{ steps.calculate.outputs.task1_score || 0 }}, "max_score": 30}'
        TASK_02_RESULTS: '{"score": ${{ steps.calculate.outputs.task2_score || 0 }}, "max_score": 40}'
        TASK_03_RESULTS: '{"score": ${{ steps.calculate.outputs.task3_score || 0 }}, "max_score": 40}'
      with:
        runners: task_01,task_02,task_03

        # TASK_01_RESULTS: '{"points": ${{ steps.calculate.outputs.task1_score || 0 }}}'
        # TASK_02_RESULTS: '{"points": ${{ steps.calculate.outputs.task2_score || 0 }}}'
        # TASK_03_RESULTS: '{"points": ${{ steps.calculate.outputs.task3_score || 0 }}}'

        # runners: "task_01:${{ steps.calculate.outputs.task1_score || 0 }},task_02:${{ steps.calculate.outputs.task2_score || 0 }},task_03:${{ steps.calculate.outputs.task3_score || 0 }}"
        # runners: "task_01:${{ steps.calculate.outputs.task1_score }},task_02:${{ steps.calculate.outputs.task2_score }},task_03:${{ steps.calculate.outputs.task3_score }}"
        # Попробуем JSON формат
        # runners: |
        #  [
        #    {"name": "task_01", "points": ${{ steps.calculate.outputs.task1_score || 0 }}},
        #    {"name": "task_02", "points": ${{ steps.calculate.outputs.task2_score || 0 }}},
        #    {"name": "task_03", "points": ${{ steps.calculate.outputs.task3_score || 0 }}}
        #  ]

  # JOB: АНАЛИЗ КАЧЕСТВА КОДА С ЛИНТЕРАМИ
  code-quality-analysis:
    name: Анализ качества кода
    runs-on: ubuntu-latest
    if: always()
    
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"

    - name: Install linters
      run: |
        echo "Installing linters..."
        pip install pylint flake8 ruff -q
        echo "Linters installed"

    - name: Run code analysis
      run: |
        # Только Python скрипт, без лишних echo
        python3 tools/code_analysis.py >> $GITHUB_STEP_SUMMARY
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 💡 Рекомендации по улучшению:" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "1. **Используйте автоматическое исправление Ruff:**" >> $GITHUB_STEP_SUMMARY
        echo '   ```bash' >> $GITHUB_STEP_SUMMARY
        echo '   ruff check --fix .' >> $GITHUB_STEP_SUMMARY
        echo '   ```' >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "2. **Следуйте PEP 8:** 4 пробела для отступов, максимум 79 символов в строке" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "3. **Проверяйте код перед отправкой**" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "*Качество кода учитывается при оценке!*" >> $GITHUB_STEP_SUMMARY